# Heartbeat

Every 1-2 minutes during the demo, check the inbox and process at most one draft. If no drafts exist, do nothing else. Keep each heartbeat turn short and rely on the JSON returned by `./bin/imem`.
